#include <stdlib.h>
// #include <ncurses.h>
#include <string.h> 

// Stuff I've gathered from the Ncurses Tutorial
//initscr(); // initialize the screen (clear it?)
//cbreak() // command keys like ctrl-c will still send the kill sig but others will pass through
//noecho(); // Do not output the users characters they type while in getch mode

//printw("Hello World !!!");
//refresh();
//getch();
//endwin(); // exit the curses screen mode

typedef struct _fields {
	
} fields;

int main(int argc, char **argv){

	return 0;
}